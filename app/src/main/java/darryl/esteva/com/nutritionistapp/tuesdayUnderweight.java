package darryl.esteva.com.nutritionistapp;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;
import android.widget.TextView;

public class tuesdayUnderweight extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_tuesday_underweight);

        /*TextView mTitleWindow = (TextView) findViewById(R.id.titleWindow);
        TextView mMessageWindow = (TextView) findViewById(R.id.messageWindow);

        mTitleWindow.setText("This is some title");
        StringBuilder stringBuilder = new StringBuilder();

        String someMessage = " This is the string that Im trying to append. ";

        for(int i = 0; i <100; i++)
        {
            stringBuilder.append(someMessage);
        }

        mMessageWindow.setText(stringBuilder.toString()) ;*/



    }
}
